# 프로젝트 룰

항상 한국어 존댓말로 답합니다.

## 유지보수 원칙

### 하드코딩 금지
- 모델명, provider, base URL, API key, 프롬프트, 점수 기준, 상태값, 라벨, 라우트 경로, 반복 사용되는 메시지를 파일 내부에 직접 박아 넣지 않습니다.
- 환경별로 달라질 수 있는 값은 `.env`, DB 설정, 설정 페이지, 상수 파일, 프롬프트 템플릿 파일 중 적절한 곳으로 분리합니다.
- 비밀값은 서버 환경변수에서만 읽고, 브라우저로 전달하지 않습니다.
- 불가피하게 고정값이 필요하면 의미 있는 이름의 상수로 분리하고, 왜 고정값인지 주석 또는 문서로 근거를 남깁니다.

### 한 파일에 여러 기능 넣기 금지
- 하나의 파일은 하나의 책임만 가집니다.
- UI 컴포넌트, API 핸들러, DB 접근, provider 호출, 프롬프트 구성, 점수 계산, 포맷 변환 로직을 한 파일에 섞지 않습니다.
- 파일이 커지거나 역할이 둘 이상으로 늘어나면 즉시 `service`, `repository`, `schema`, `constants`, `components`, `utils` 등으로 분리합니다.
- 페이지 파일은 화면 조립과 데이터 전달 중심으로 유지하고, 복잡한 비즈니스 로직은 별도 모듈로 이동합니다.

### 중복 코드 금지
- 비슷한 기능을 새로 작성하기 전에 기존 함수, 컴포넌트, 상수, schema, 유틸을 먼저 찾습니다.
- 같은 검증 규칙, fetch 처리, 에러 응답, 상태 라벨, 점수 계산, textarea/input UI가 반복되면 공통 모듈로 추출합니다.
- 복사해서 조금 수정하는 방식은 금지합니다. 차이가 있는 부분만 파라미터나 설정 객체로 분리합니다.
- 단, 성급한 추상화로 의미가 다른 기능을 억지로 묶지 않습니다. “같은 이유로 바뀌는 코드”만 공통화합니다.

## 구현 기준

- 모든 모델 생성 요청은 서버 API route를 거쳐야 합니다.
- 프론트엔드에서 모델 API를 직접 호출하지 않습니다.
- 자동 발행, 대량 글 생성, ChatGPT 웹 자동 조작, OAuth 토큰 저장은 구현하지 않습니다.
- 외부 데이터가 없는 화제성 점수는 반드시 추정 점수임을 데이터와 UI에 표시합니다.
- 새 기능을 추가할 때는 DB 저장 여부, 새로고침 후 유지 여부, GenerationLog 기록 여부를 함께 확인합니다.
